name: Node.js CI

on:
  push:
    branch: main
  pull_request:
    branch: main

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10.x, 12.x, 14.x, 15.x]
      
    steps:
    - run: npm test